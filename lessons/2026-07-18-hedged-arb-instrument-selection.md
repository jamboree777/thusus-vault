---
type: lesson
date: 2026-07-18
trigger: "39% livescan capture + instrument-selection design — livescan keeps only ~39% of quoted edge because the 15-60min transit window is unhedged (the measured 61% decay, 121-trade sample). Robin: hedge the arb at the RICH venue so the edge is locked at t=0, and pick the hedge instrument per trade."
problem: "An unhedged transfer arb carries its full price risk across the transit window, so most of the quoted edge evaporates before the coins arrive to be sold. The engine had no way to (a) neutralise that window nor (b) prove, trade-by-trade, how much edge hedging actually recovers. There was also no honest per-trade choice of HOW to hedge — margin short vs perp short have different cost structures and coverage, and the cheaper one flips by token."
change: "New shadow engine nw_hedged_shadow.py (strategy #4, 'Covered Arb'). For each arb candidate (cheap venue buy → rich venue sell) it picks a hedge AT THE RICH VENUE by min INCREMENTAL effective cost: ① MARGIN SHORT (borrow+sell spot at t=0; transferred coin repays) — incremental cost = borrow interest × hold ONLY (the spot sell fee is NOT incremental: the unhedged baseline also pays a sell fee on arrival, margin just moves it to t=0), gate APR fetched LIVE (gate /margin/uni/estimate_rate), other venues conservative env tier; a preference bonus (NW_HEDGE_MARGIN_PREF_PCT) favours ① because it locks the venue premium itself; ② PERP SHORT (rich venue if listed else most-liquid of gate/bybit/binance — NEVER mexc[frozen order API]/bitget[Korea]) — incremental cost = 2× taker + expected funding (sign-aware: short RECEIVES when funding>0; rate+interval fetched PER CONTRACT, never hard-coded 8h); ③ NEITHER → recorded as unhedged-comparable skip. Candidates reuse the live feeds (/arb/board global-global incl. thin names above NW_HEDGE_MIN_EDGE_PCT=0.5, and /arb/kimchi_roundturn discount-mode = buy Korea cheap + hedge overseas, korea_leg tagged paper-only-realistic). PERP CONTRACT QUANTIZATION: hedge_qty = floor(spot_qty/step)×step respecting min qty/notional → hedge_coverage_pct (perp can be <100%, 0 if below min contract → perp not viable); coverage <NW_HEDGE_MIN_COVER_PCT=70 loses to margin or skips. Settle at the per-chain clock (chain_settle_delay imported from nw_paper_arb) records the A/B: realized_hedged vs realized_unhedged (same entry, no hedge, sold at arrival) → capture_hedged vs capture_unhedged. Execution protocol (COVERED_ARB_EXECUTION.md) subset: T-0 entry-check stamps (live VWAP re-check, instrument/dw check, basis-adjusted premium truth, modeled margin headroom), in-flight invariant (qty_short==qty_in_flight ± dust; perp residual is intentional not a violation), exit modes E1 normal / E2 early (pre-arrival premium collapse) / E5 timestop (max(chainETA×2,24h)), and E3/E4/E6 stamped as scenario tail costs. Own table nw_hedged_shadow; joined into /thusus book as strategy 'hedged' + additive GET /arb/hedged_shadow. Capital model SHADOW-ONLY — does NOT move the $10k pools yet."
expected_effect: "Hedged capture should sit far above the ~39% unhedged baseline (target ≥85% per GLOBAL_BORROW_MAP §6 gate) — the per-trade A/B (capture_hedged vs capture_unhedged) is the direct evidence. Cost hierarchy is NOT fixed and is resolved per trade: majors ~0.002%/4h incremental margin interest usually cheapest; hot alts whose borrow spikes 50-200% APR → perp wins; bithumb daily-ceil always priciest. Perp coverage is the honest catch: the $40-300 band makes min-contract/min-notional bite often on majors (BTC lots), so many perp candidates cover <70% and correctly lose to margin — a real advantage of margin shorts on small positions. First passes will be THIN; the passes accumulate the A/B table."
review_after: 2026-07-25
status: active
supersedes: null
---

# Hedge the transit window — and choose the cheaper hedge per trade

Livescan's honesty is also its indictment: it settles a transfer arb against the *live* book at the far end of the transit window, and across 121 trades that window destroys **61%** of the quoted edge — realized retention **~39%**. The coin is in the air for 15-60 minutes with its full price risk live, and by the time it lands the premium that justified the trade has mostly converged.

Strategy #4 removes that window's *price risk* without removing the transfer: hedge the position **at the rich venue at t=0** so the edge is locked immediately, then let the coin settle into a market-neutral book. The whole engine is a shadow whose deliverable is the **A/B**: for every settled candidate it records both the hedged realization and what the *unhedged* path (same entry, no hedge, sold at arrival) would have realized. `capture_hedged` vs `capture_unhedged` is the proof.

## The instrument is a per-trade decision, not a constant

Two ways to cover, with different economics, and the cheaper one flips by token:

- **① Margin short** — borrow the token at the rich venue and sell it spot at t=0; the transferred coin repays the loan. This locks the *venue premium itself* and covers ~100% (spot has fine granularity). Its **incremental** cost is borrow interest over the hold **only** — the spot sell fee is *not* a cost of hedging, because the unhedged baseline pays a sell fee on arrival too; margin merely moves that same sale to t=0. Charging the taker here would have made margin look falsely expensive and wrongly biased the selector toward perp. Gate's borrow APR is pulled **live** (public estimate-rate); other venues use a conservative tier and log `source=assumed`.
- **② Perp short** — hedges market drift only (the premium-convergence risk remains), costs 2× taker + funding, and funding **sign matters**: a short *receives* funding when the rate is positive, so the current rate + interval are fetched *per contract* (never the hard-coded 8h). Never mexc (order API frozen since 2022), never bitget (Korea-restricted).

A preference bonus favours ① because it locks strictly more. The cost hierarchy is **not fixed**: majors borrow at a rounding error (~0.002% over a 4h hold) so margin wins; a hot alt whose borrow spikes to 50-200% APR flips it to perp; bithumb's daily-ceil lending is always the priciest. That is exactly why the comparison is live and per-trade.

## Perps are quantized — coverage is the honest catch

Futures trade in whole contracts (contract size / lot step / min order), so a perp short cannot cover an arbitrary spot amount: `hedge_qty = floor(spot_qty/step)×step`, and `hedge_coverage_pct = hedged_notional / spot_notional` can be **well under 100% — or 0** when the spot size is below the min contract. Our **$40-300** band makes this bite *often* on majors (a $200 BTC position is a fraction of one BTC perp lot). A perp whose best-size coverage falls below **NW_HEDGE_MIN_COVER_PCT (70%)** loses to margin or falls to skip — a concrete, measured **advantage of margin shorts on small positions**. The uncovered residual is not hidden: at settle the perp P&L is computed on `hedge_qty` only and the residual rides the drift inside the arrival spot sale — the realized is an honest **blend**.

## Shadow discipline

Capital model is **shadow-only** — nothing moves the $10k pools yet (that joins after the A/B validates). The engine stamps the T-0 execution checklist (live VWAP re-check, instrument/dw checks, basis-adjusted premium truth, modeled +30% margin headroom), asserts the position invariant each pass (`qty_short == qty_in_flight ± dust`; a perp's intentional residual is *not* a violation, an over-hedge is and screams), and models exit modes E1/E2/E5 as events with E3/E4/E6 stamped as scenario tail costs. Every row carries its instrument decision, live rates, funding sign, coverage, and exit mode so any trade is fully attributable.

## Net-gate fix — gate on NET, not GROSS (2026-07-18)

Robin caught the entry gate admitting trivial-**NET** trades. `NW_HEDGE_MIN_EDGE_PCT=0.5` gates on the **gross** quoted edge, but the covered position pays cover cost (~0.1% interest/funding) + taker fees on all legs (~0.2%) + basis before it nets anything. A 0.5% *gross* trade nets only **~0.1–0.2%** — measured examples: **XCN net 0.09%**, **ARX net 0.22%**. That is not worth the 4-leg execution + margin-call/liquidation tail + exit-complexity risk. The old binding condition was even looser: the size loop accepted any size with `net_pct >= 0.0`, so anything barely positive booked.

The fix borrows **won-carry's** net-first discipline, with a single binding knob:

- **Gross `MIN_EDGE_PCT` is now a cheap PREFILTER only** — candidates must clear it before we VWAP-walk any book (so we don't price every row), but it is *not* the binding gate.
- **The SOLE BINDING gate is expected NET%**: `NW_HEDGE_MIN_NET_PCT` (default **0.5**). A candidate is admitted only if some size gives `net_pct >= MIN_NET_PCT`. The net is the value `_entry_economics_for_size` already computes — `gross − cover_cost − buy taker − hedge/sell taker(s) − wd/basis` — the **SAME** routine whose expectation the settle realizes, so the gate and the settle agree by construction (no second formula).
- **No absolute-$ floor.** An earlier draft added a `MIN_NET_USD` ($1) floor; Robin removed it — *"the $1 minimum is not important."* A small-size trade that clears 0.5% net still qualifies, which is the **diversification intent** (spread capital across many small positions rather than demand a dollar minimum that biases toward large size).

Why 0.5% net is the right conservative default: hedged is **market-neutral** (no FX/cycle-lock risk, unlike won-carry), so 0.5% net is *conservative* and env-tunable — but the covered position still carries 4-leg execution slippage, the margin-call/liquidation tail (E4), contract-residual drift, and exit-mode tail costs (E3/E4/E6), so a real net floor is prudent. It can be **lowered later** once accumulated A/B shows those tail costs are benign.

**Retroactive purge (not forward-only).** The pre-fix code had booked trivial-net rows (XCN 0.101%, ARX 0.114%, CFG 0.143%, MET 0.442%, XVS 0.465%, KAITO 0.417%, SAPIEN 0.488%). Robin wanted them gone, so every already-booked `nw_hedged_shadow` row with `expected_net_pct < 0.5` was **DELETEd** (shadow table only — never `nw_paper_trades` or other engines). So both the A/B surface and the live gate now hold to the 0.5% net floor.

Instrumentation: each pass now logs `net_gate: pass=N drop=M(<0.5%)` — a candidate is counted `drop` when it had a feasible positive-net size (the old `net>=0` gate would have entered) but no size cleared the floor.

## Review

By 2026-07-25: does `nw_hedged_shadow` show `capture_hedged` materially above `capture_unhedged` (target hedged retention ≥ ~0.85 vs the ~0.39 unhedged baseline)? What is the margin/perp/skip split, and how often does perp lose on coverage <70%? Is the live gate borrow rate diverging from the conservative default on hot alts (the signal that per-trade live pricing matters)? First passes will be thin — the value is the accumulating A/B.

_Related: [[five-min-settlement]] · [[executable-spread]] · [[transfer-feasibility]] · [[quiet-size]] · [[2026-07-18-per-chain-settle-delay]] · [[2026-07-17-woncarry-exit-policy-v2]] · [[GLOBAL_BORROW_MAP]] · [[BITHUMB_LENDING_ARB]] · [[COVERED_ARB_EXECUTION]] · [[THUSUS_OPS_LOOP]] · [[Thusus]]_
