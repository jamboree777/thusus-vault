# Draft — X / Twitter thread (EN)

> Maintainer note (not for posting): DRAFT, 7 tweets. Robin's call on timing. One hook line per tweet; keep each under ~270 chars. Attach a graph-view GIF to tweet 1.

---

**1/**
Most crypto data sites sell you numbers.
Nobody sells the *meaning* — why a gap exists, why deposits froze, what actually happened during an incident.
So I built a public Obsidian vault for exactly that. Your AI can read it. 🧵

**2/**
It's a real vault: plain markdown, typed frontmatter, `[[wikilinks]]`.
`git clone` → Open as vault → the graph lights up: tokens ↔ exchanges ↔ chains ↔ concepts ↔ a daily journal.
No app, no login. Just files.

**3/**
The weird part: a bot writes *into* it every night (03:50 UTC) and it stays hand-editable.
Each note has a machine region between `nw:auto` markers. The bot only rewrites inside them. Your prose outside survives forever.
Living dataset, not a stale dump.

**4/**
Frontmatter is a published contract — typed keys for grade, transfer status, contracts.
So Dataview / Bases queries work on a fresh clone, and any LLM can parse the files without scraping.
The frontmatter spec *is* the API.

**5/**
Anyone can contribute by PR: drop a claim with a mandatory source URL, a GitHub Action schema-checks it, review verifies the source actually says it.
Unverified claims NEVER render as fact. Verified ones earn Cherry + a citable contributor page.

**6/**
Honest by design: the trading journal is a PAPER / dry-run book — simulated fills vs real quotes, labelled everywhere. Not a track record, not advice.
Prose is CC BY-NC-SA 4.0, attribution NightWatch / Thusus. Forks stay open.

**7/**
Grown from a 49-file seed to ~100 notes, updated nightly.
Clone it and open it in your own vault:

```
git clone https://github.com/jamboree777/thusus-vault
```

→ https://github.com/jamboree777/thusus-vault
