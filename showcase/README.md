# Showcase kit — maintainer notes (Robin)

These are **drafts you publish, not the worker.** Nothing here has been posted anywhere. Posting is entirely your call — pick the channel, the timing, and edit the voice to yours first.

## What's in here

| File | Where it's for |
|---|---|
| [`reddit-obsidianmd.md`](reddit-obsidianmd.md) | r/ObsidianMD — short, Obsidian-native angle, 3 title options |
| [`obsidian-forum.md`](obsidian-forum.md) | Obsidian forum → **Share & showcase** category — longer, architectural |
| [`x-thread.md`](x-thread.md) | X/Twitter — 7-tweet thread, ends with the clone command |

## Suggested order & timing

1. **Wait for the first outside signal.** A single external star or clone is enough — it means the "clone it" CTA lands before an audience arrives. No need to wait for more.
2. **Obsidian forum first** (most forgiving, values depth), **then r/ObsidianMD** (respects low-hype, punishes marketing), **then the X thread** (broadest, least context — post last so the repo already has a star or two).
3. Space them out over a few days; don't cross-post the same hour.

## Screenshots to take first (exact targets)

Take these before posting — the drafts reference them:

- **Graph view** of the whole vault (the hero image). Zoom so `concepts/mirage-arb.md` and `concepts/one-price.md` show as hubs.
- **`tokens/BLAST.md`** open in Obsidian, scrolled to the boundary between the `<!-- nw:auto:end -->` marker and the hand-written "Prose (durable)" section — this shows the machine-region / prose-region split in one frame.
- **`claims/pending/2026-07-16-blast-upbit-deposit-resumed.md`** — a real source-cited claim.
- **A PR page** showing the green `validate-claims` check (the schema Action) — proves the contribution loop is real, not aspirational. Re-open a claim PR to capture this.
- Optional Dataview screenshot: a small query over `tokens/` frontmatter (e.g. grade + transfer status table) to show the frontmatter-as-API claim.

## Fact-check before you hit post

- File count: repo is ~100 notes (95 on `main` + this kit) — the drafts say "~100"; bump the number if it's grown.
- The "first contributor paid 10 Cherry" / "verified contributor page" line: confirm the contributor page exists under `entities/contributors/` before you screenshot it. If it isn't live yet, keep the sentence but don't promise a screenshot of it.
- Daily sync time (03:50 UTC) and the repo URL are correct as of this writing — re-verify if the bot schedule changed.

## Guardrails baked into every draft

- Honest tone, no hype. Every draft states PAPER / dry-run and the CC BY-NC-SA license up front.
- Never oversell the trading — it's simulated, it is not a track record, it is not advice.
- Do not post as if from the NightWatch team without saying so; the vault persona is "Thusus", and the human+AI co-authorship is part of the story, not something to hide.
